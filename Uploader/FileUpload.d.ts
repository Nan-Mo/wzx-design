/// <reference types="react" />
import './index.less';
import { UploadProps } from '.';
declare const FileUploader: ({ accept, action, multiple, disabled, limitSize, beforeUpload, showUploadList, uploading, setUploading, value, onChange, isSingle, additionalData, className, fileName, maxCount, }: UploadProps) => JSX.Element;
export default FileUploader;
